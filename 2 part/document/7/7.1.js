//createTextNode vs innerHTML vs textContent

// У нас есть пустой DOM-элемент elem и строка text.

// Какие из этих 3-х команд работают одинаково?

// elem.append(document.createTextNode(text)) //в контент вставится текст с тегами

// elem.innerHTML = text  //текст втавится без тегов
// elem.textContent = text  //в контент вставится текст с тегами
